@model IEnumerable<DayThree.Models.Book>

@{
    ViewBag.Title = "Book List";
}

<h2>@ViewBag.Title</h2>



<p>
    @Html.ActionLink("Create New", "Create", "Books")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsOnSale)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Author)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsOnSale)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Author)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>

@using DayThree.Extensions.CustomHtmlHelpers

<p> Povik na custom helperot za baner </p>
@Html.ImageBanner("SALE 60% ", "http://4.bp.blogspot.com/_yj3dsjeSM-w/TIMrvxjv6LI/AAAAAAAAAU0/ETE7yP_PmsE/s1600/bookstore.gif", "alt", "250", "150")
<br>
<p> Povik na custom helperot za kreiranje labela so vrednost so golemi bukvi</p>
@Html.LabelToUpper("make upper")
@Html.ImageBanner("Everybody wants to be a hero ", "https://i.ytimg.com/vi/sf9hdOMs8RQ/maxresdefault.jpg", "alt", "250", "150")